services:
  app:
    build:
      context: .
    image: mcmillanator/gemstash
    hostname: gemstash
    restart: always
    volumes:
      - gemstash:/home/.gemstash
    networks:
      - docker_default

volumes:
  gemstash:

networks:
  docker_default:
    external: true
